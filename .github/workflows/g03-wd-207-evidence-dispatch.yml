name: G03 WD 207 evidence dispatch
on:
  push:
    branches:
      - ops/g03-wd-207-evidence-dispatch-20260917
permissions:
  actions: write
  contents: read
jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch canonical history sprint for WD 207 evidence
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl --fail-with-body -sS -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/history-sprint.yml/dispatches" \
            -d '{"ref":"main","inputs":{"max_issues":"1","max_translations":"120","categories":"browser","source_run_id":"","state_source_run_id":"","strict_final":"false","evidence_issue_ids":"wd-207-c","capture_wiley_evidence":"false"}}'
