name: Backfill TOP5 history (deprecated)

# The unified field-history workflow covers all 49 journals, including TOP5.
# Keep this dispatch entry as a visible migration notice only. It has read-only
# permissions and contains no data-branch checkout, publication, or push path.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: journal-data-update
  cancel-in-progress: false

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Point operators to the unified backfill
        run: |
          echo "This workflow is retired."
          echo "Run 'Backfill field journal history' for TOP5 and field journals."
