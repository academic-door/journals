name: Controlled history evidence dispatch

on:
  issue_comment:
    types: [created]

jobs:
  validate:
    if: >-
      github.event.issue.number == 265 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      startsWith(github.event.comment.body, '/history-evidence')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      issue_id: ${{ steps.validate.outputs.issue_id }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
      - name: Validate bounded history-evidence request
        id: validate
        shell: bash
        run: >-
          python scripts/validate_history_evidence_dispatch.py
          "$GITHUB_EVENT_PATH"
          --repo-root "$GITHUB_WORKSPACE"
          --github-output "$GITHUB_OUTPUT"

  history:
    needs: validate
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "1"
      max_translations: "120"
      categories: browser
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: ${{ needs.validate.outputs.issue_id }}
      capture_wiley_evidence: false
    secrets: inherit
  
  validate_browser_batch:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      startsWith(github.event.comment.body, '/history-browser-batch ')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      batch_id: ${{ steps.validate_browser_batch.outputs.batch_id }}
      issue_ids: ${{ steps.validate_browser_batch.outputs.issue_ids }}
      issue_count: ${{ steps.validate_browser_batch.outputs.issue_count }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
      - name: Validate bounded publisher-family browser evidence batch
        id: validate_browser_batch
        shell: bash
        run: >-
          python scripts/validate_history_browser_batch_dispatch.py
          "$GITHUB_EVENT_PATH"
          --repo-root "$GITHUB_WORKSPACE"
          --github-output "$GITHUB_OUTPUT"

  browser_batch:
    needs: validate_browser_batch
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "12"
      max_translations: "120"
      categories: browser
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: ${{ needs.validate_browser_batch.outputs.issue_ids }}
      capture_wiley_evidence: false
      repair_dates_only: false
      repair_content_only: false
    secrets: inherit

  date_repair:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-date-repair'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "1"
      max_translations: "1"
      categories: __date_repair_only__
      source_run_id: ""
      state_source_run_id: ""
      strict_final: false
      evidence_issue_ids: ""
      capture_wiley_evidence: false
      repair_dates_only: true
    secrets: inherit

  wave_a:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-wave-a'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "10"
      max_translations: "120"
      categories: source_pending,translation_required
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: ""
      capture_wiley_evidence: false
      repair_dates_only: false
    secrets: inherit

  translation_only:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-translation-only'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "10"
      max_translations: "120"
      categories: translation_required
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: ""
      capture_wiley_evidence: false
      repair_dates_only: false
      repair_content_only: false
    secrets: inherit

  wave_b:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-wave-b'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "10"
      max_translations: "120"
      categories: recoverable
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: ""
      capture_wiley_evidence: false
      repair_dates_only: false
    secrets: inherit

  ready_contract_repair:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-ready-contract-repair'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "1"
      max_translations: "1"
      categories: __ready_contract_repair_only__
      source_run_id: ""
      state_source_run_id: ""
      strict_final: false
      evidence_issue_ids: ""
      capture_wiley_evidence: false
      repair_dates_only: false
      repair_content_only: false
      repair_ready_contract_only: true
    secrets: inherit

  content_repair:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-content-repair'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "1"
      max_translations: "1"
      categories: __content_repair_only__
      source_run_id: ""
      state_source_run_id: ""
      strict_final: false
      evidence_issue_ids: ""
      capture_wiley_evidence: false
      repair_dates_only: false
      repair_content_only: true
    secrets: inherit

  chicago_evidence:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-chicago-evidence'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "1"
      max_translations: "120"
      categories: __chicago_evidence_only__
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: >-
        jaere-10-1,jaere-10-2,jaere-10-3,jaere-10-4,jaere-10-5,jaere-10-6,
        jaere-11-1,jaere-11-2,jaere-11-3,jaere-11-4,jaere-11-5,jaere-11-6,jaere-11-S1,
        jle-41-1,jle-41-2,jle-41-3,jle-41-4,jle-41-S1,
        jle-42-1,jle-42-2,jle-42-3,jle-42-4,jle-42-S1,jle-43-S1
      capture_wiley_evidence: false
      repair_dates_only: false
      repair_content_only: false
    secrets: inherit

  cambridge_evidence:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-cambridge-evidence'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "1"
      max_translations: "120"
      categories: __cambridge_evidence_only__
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: "cer-80-c,cer-94-c"
      capture_wiley_evidence: false
      capture_cambridge_evidence: true
      repair_dates_only: false
      repair_content_only: false
      repair_ready_contract_only: false
    secrets: inherit

  wiley_evidence:
    if: >-
      github.event.issue.number == 205 &&
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == 'SIMON-WORLD' &&
      github.event.comment.body == '/history-wiley-evidence'
    uses: ./.github/workflows/history-sprint.yml
    permissions:
      actions: read
      contents: write
      models: read
    with:
      max_issues: "1"
      max_translations: "120"
      categories: __wiley_evidence_only__
      source_run_id: ""
      state_source_run_id: "32734420419"
      strict_final: false
      evidence_issue_ids: >-
        ajae-107-1,ajae-107-2,ajae-107-3,ajae-107-4,ajae-107-5,
        jf-80-1,jf-80-2,jf-80-3,jf-80-4,jf-80-5,jf-80-6,
        ier-64-1,ier-64-2,ier-64-3
      capture_wiley_evidence: false
      repair_dates_only: false
      repair_content_only: false
    secrets: inherit
